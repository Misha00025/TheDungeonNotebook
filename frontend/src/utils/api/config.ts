// API Configuration

// Base URLs
export const API_BASE_URL = "https://thedun.ru/api";
export const AUTH_CALLBACK_URL = "https://thedun.ru/login";

// API Versions
export const API_VERSIONS = {
  V1: "v1",
};

// Endpoints
export const ENDPOINTS = {
  AUTH: "/auth",
  GROUPS: "/groups",
  NOTES: "/notes",
  ITEMS: "/items",
};

// Request timeout in milliseconds
export const REQUEST_TIMEOUT = 30000;

// Storage keys
export const STORAGE_KEYS = {
  ACCESS_TOKEN: "accessToken",
};
