# Notes Service (deprecated)

> ⚠️ **Сервис упразднён.** Функционал заметок перенесён в `campaign-service`.
> 
> Все эндпоинты обслуживаются через API Gateway по адресам `/groups/{groupId}/notes*` и `/groups/{groupId}/characters/{characterId}/notes*`.
>
> См. `backend/campaign-service/README.md` — секция **Заметки**.
